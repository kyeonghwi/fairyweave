---
phase: 01-project-setup-gemini-api-spike
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tailwind.config.ts
  - frontend/postcss.config.js
  - frontend/next.config.ts
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - backend/package.json
  - backend/tsconfig.json
  - backend/src/index.ts
  - backend/.env.example
  - backend/.env
autonomous: true
requirements: []

must_haves:
  truths:
    - "`npm run dev` at repo root starts both frontend (port 3000) and backend (port 3001) concurrently"
    - "GET http://localhost:3000 responds (Next.js placeholder page)"
    - "GET http://localhost:3001/health responds with 200 JSON"
    - "backend/.env contains GEMINI_API_KEY= entry (value filled by developer)"
  artifacts:
    - path: "package.json"
      provides: "Root dev script using concurrently"
      contains: "concurrently"
    - path: "frontend/src/app/page.tsx"
      provides: "Next.js App Router root page"
      contains: "FairyWeave"
    - path: "backend/src/index.ts"
      provides: "Express app entry point"
      contains: "app.listen"
    - path: "backend/.env.example"
      provides: "Env var template"
      contains: "GEMINI_API_KEY="
    - path: ".gitignore"
      provides: "Git ignore rules"
      contains: ".env"
  key_links:
    - from: "package.json"
      to: "frontend/ + backend/"
      via: "concurrently npm scripts"
      pattern: "concurrently"
    - from: "backend/src/index.ts"
      to: "port 3001"
      via: "app.listen(3001"
      pattern: "app\\.listen\\(3001"
---

<objective>
Initialize the monorepo skeleton: root scripts, Next.js 14 frontend with Tailwind, and Express+TypeScript backend. No Gemini code yet — this plan creates the runnable foundation that Plan 2 writes routes into.

Purpose: Every subsequent phase depends on this skeleton. Get it running before touching any AI integration.
Output: Two runnable processes (frontend dev server, backend dev server) launched from one `npm run dev` at the repo root.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-project-setup-gemini-api-spike/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize root workspace and backend</name>
  <read_first>
    - D:/Fairyweave/.planning/phases/01-project-setup-gemini-api-spike/01-CONTEXT.md (decisions section)
  </read_first>
  <files>
    package.json
    .gitignore
    backend/package.json
    backend/tsconfig.json
    backend/src/index.ts
    backend/.env.example
    backend/.env
  </files>
  <action>
Create the root `package.json` and the full `backend/` scaffold.

**Root package.json** (D:/Fairyweave/package.json):
```json
{
  "name": "fairyweave",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
```

**Root .gitignore** (D:/Fairyweave/.gitignore):
```
node_modules/
frontend/node_modules/
backend/node_modules/
.env
frontend/.env
backend/.env
.env.local
frontend/.env.local
*.env.local
.next/
dist/
```

**backend/package.json**:
```json
{
  "name": "fairyweave-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.15.7",
    "typescript": "^5.5.2"
  }
}
```

**backend/tsconfig.json**:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "lib": ["ES2022"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**backend/src/index.ts**:
```typescript
import express from 'express';
import cors from 'cors';
import dotenv from 'dotenv';

dotenv.config();

const app = express();
const PORT = 3001;

app.use(cors());
app.use(express.json());

app.get('/health', (_req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

// Routes will be registered here in Plan 2
// app.use('/api', generateRouter);

app.listen(PORT, () => {
  console.log(`Backend running on http://localhost:${PORT}`);
});

export default app;
```

**backend/.env.example**:
```
GEMINI_API_KEY=
```

**backend/.env** — create this file with:
```
GEMINI_API_KEY=
```
(Developer fills in actual key before running Plan 2 tests)

After creating all files, run:
```bash
cd D:/Fairyweave && npm install
cd D:/Fairyweave/backend && npm install
```
  </action>
  <verify>
    <automated>cd D:/Fairyweave/backend && npx tsx src/index.ts &amp; sleep 3 &amp;&amp; curl -s http://localhost:3001/health &amp;&amp; kill %1</automated>
  </verify>
  <acceptance_criteria>
    - `D:/Fairyweave/package.json` contains the string `"concurrently"`
    - `D:/Fairyweave/.gitignore` contains the line `.env`
    - `D:/Fairyweave/backend/src/index.ts` contains `app.listen(3001`
    - `D:/Fairyweave/backend/.env.example` contains exactly `GEMINI_API_KEY=`
    - `D:/Fairyweave/backend/.env` exists (even if value is empty)
    - `curl http://localhost:3001/health` returns JSON with `"status":"ok"`
  </acceptance_criteria>
  <done>Backend starts on port 3001, /health returns 200 JSON, all config files present.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Next.js frontend with Tailwind</name>
  <read_first>
    - D:/Fairyweave/.planning/phases/01-project-setup-gemini-api-spike/01-CONTEXT.md (decisions section — frontend)
  </read_first>
  <files>
    frontend/package.json
    frontend/tsconfig.json
    frontend/next.config.ts
    frontend/tailwind.config.ts
    frontend/postcss.config.js
    frontend/src/app/layout.tsx
    frontend/src/app/page.tsx
    frontend/src/app/globals.css
  </files>
  <action>
Scaffold the Next.js 14 App Router frontend. Use `create-next-app` if available, otherwise create files manually.

**Preferred approach — run create-next-app:**
```bash
cd D:/Fairyweave && npx create-next-app@latest frontend \
  --typescript \
  --tailwind \
  --app \
  --src-dir \
  --no-eslint \
  --no-import-alias \
  --use-npm
```

If `create-next-app` succeeds, it handles all config files. Then replace the generated `frontend/src/app/page.tsx` with the minimal placeholder below.

**If create-next-app is not available**, create these files manually:

**frontend/package.json**:
```json
{
  "name": "fairyweave-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000"
  },
  "dependencies": {
    "next": "^14.2.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.5.2"
  }
}
```

**frontend/tsconfig.json**:
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }]
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**frontend/next.config.ts**:
```typescript
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {};

export default nextConfig;
```

**frontend/tailwind.config.ts**:
```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
```

**frontend/postcss.config.js**:
```js
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

**frontend/src/app/globals.css**:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

**frontend/src/app/layout.tsx**:
```tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'FairyWeave',
  description: '세상에 단 하나뿐인 동화책',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  );
}
```

**frontend/src/app/page.tsx** (always write this, even after create-next-app):
```tsx
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="text-4xl font-bold">FairyWeave</h1>
    </main>
  );
}
```

After creating files, run:
```bash
cd D:/Fairyweave/frontend && npm install
```
  </action>
  <verify>
    <automated>cd D:/Fairyweave/frontend && npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `D:/Fairyweave/frontend/src/app/page.tsx` contains the string `FairyWeave`
    - `D:/Fairyweave/frontend/tailwind.config.ts` contains `src/app/**/*.{js,ts,jsx,tsx,mdx}`
    - `D:/Fairyweave/frontend/src/app/globals.css` contains `@tailwind base`
    - `npm run build` inside `frontend/` exits with code 0 (no TypeScript or Next.js errors)
    - `D:/Fairyweave/frontend/package.json` contains `"next"` in dependencies
  </acceptance_criteria>
  <done>Frontend builds cleanly, page.tsx shows FairyWeave placeholder, Tailwind configured.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. From repo root: `npm install` — no errors
2. Start backend: `cd backend && npx tsx src/index.ts` — prints "Backend running on http://localhost:3001"
3. `curl http://localhost:3001/health` — returns `{"status":"ok",...}`
4. Start frontend: `cd frontend && npm run dev` — Next.js starts on port 3000
5. `curl http://localhost:3000` — returns HTML containing "FairyWeave"
6. Root `npm run dev` launches both via concurrently
</verification>

<success_criteria>
- Root `npm run dev` starts both processes without error
- `localhost:3001/health` returns 200 JSON
- `localhost:3000` serves the FairyWeave placeholder page
- `backend/.env.example` contains `GEMINI_API_KEY=`
- `.gitignore` prevents `.env` files from being tracked
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-gemini-api-spike/01-plan-1-SUMMARY.md`
</output>
