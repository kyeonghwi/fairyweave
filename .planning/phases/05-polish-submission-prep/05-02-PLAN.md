---
phase: 05-polish-submission-prep
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - docs/submission-log.md
autonomous: false
requirements:
  - DEV-02
  - DEV-03

must_haves:
  truths:
    - "submission-log.md 구글폼 4문항이 모두 완성되어 있다"
    - "문항당 3~5줄 분량이다"
    - "문항 2에 SDK DX, 5-step 복잡도, 샌드박스 경험, 개선 제안이 포함되어 있다"
    - "문항 4에 트러블슈팅 기록 기반 실패 사례가 포함되어 있다"
    - "더미 데이터 경로 수동 테스트 체크리스트가 기록되어 있다"
  artifacts:
    - path: "docs/submission-log.md"
      provides: "Complete submission document"
      contains: "문항 2: API를 써보고 느낀 점"
  key_links:
    - from: "docs/submission-log.md"
      to: "README.md"
      via: "submission-log references same API endpoints and AI tools as README"
      pattern: "Sweetbook"
---

<objective>
Finalize submission-log.md: complete all 4 Google Form essay answers and record manual test results.

Purpose: 구글폼 제출 준비 완료
Output: docs/submission-log.md fully populated
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-submission-prep/05-CONTEXT.md
@.planning/phases/05-polish-submission-prep/05-01-SUMMARY.md
@docs/submission-log.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Google Form answers in submission-log.md (D-06~D-09)</name>
  <files>docs/submission-log.md</files>
  <read_first>
    - docs/submission-log.md (current state with drafts and troubleshooting records)
    - .planning/STATE.md (decisions log for content)
    - .planning/phases/05-polish-submission-prep/05-CONTEXT.md (D-06~D-09 tone/content guidance)
  </read_first>
  <action>
Update `docs/submission-log.md` to complete all 4 Google Form answers. Per D-06: technical + practical tone. Per D-07: 3~5 lines per answer.

**문항 1: 과제 수행 과정** — Replace current draft with complete version:

```
Phase 1에서 Next.js 16 + Express 모노레포를 구성하고 Gemini 2.5 Flash API 연동을 검증했습니다. Phase 2에서 메타-프롬프트 엔지니어링으로 16페이지 스토리 JSON 생성 파이프라인을 만들고, Promise.allSettled로 16장 삽화를 병렬 생성했습니다. Phase 3에서 Sweetbook SDK로 5-step 책 생성(create → upload → cover → contents → finalize) + 주문 플로우를 구현했습니다. Phase 4에서 프론트엔드 UI 3페이지(홈/생성/책 프리뷰+주문)와 더미 데이터 5권을 완성했습니다. Phase 5에서 README, .env.example, 제출 문서를 정리했습니다.
```

**문항 2: API를 써보고 느낀 점** — Write new content per D-09 pointers:

```
SDK에 TypeScript 타입 정의가 없어서 메서드 시그니처를 추측하며 .d.ts를 직접 작성해야 했습니다. books.create → photos.upload × 16 → covers.create → contents.insert × 16 → books.finalize로 이어지는 5단계 생성 플로우는 유연하지만 진입 장벽이 높습니다. 한 번의 API 호출로 책을 생성할 수 있는 convenience 메서드가 있으면 개발 속도가 크게 개선될 것입니다. 샌드박스 환경에서 creationType: 'TEST'로 과금 없이 테스트할 수 있어 안심하고 반복 실험이 가능했습니다. multipart 업로드 시 Blob이 아닌 File 객체를 요구하는 점은 문서에 명시되어 있지 않아 디버깅에 시간이 걸렸습니다.
```

**문항 3: 가장 중요한 판단** — Keep existing entries and ensure formatting is clean. The current content in submission-log.md already has 6 good entries. Clean up to 3~5 lines total summary format:

```
가장 중요한 판단은 Gemini JSON 파싱 전략이었습니다. LLM 출력이 항상 완벽한 JSON이 아니라는 전제로, responseMimeType 강제 + 배열 추출 + trailing comma 제거의 3중 방어를 설계했습니다. 두 번째는 AI 생성 엔드포인트를 Next.js 프록시 대신 백엔드 직접 호출로 분리한 것입니다. 수 분 걸리는 요청에 프록시 타임아웃이 맞지 않았고, 장시간 요청만 분리하여 나머지 API는 프록시의 편의성을 유지했습니다.
```

**문항 4: AI 도구 사용 중 겪은 실패** — Replace current draft with complete version based on troubleshooting records (8 entries in submission-log.md):

```
Gemini 2.0 Flash가 신규 계정에서 404를 반환하여 ListModels API로 사용 가능 모델을 확인한 뒤 2.5 Flash로 교체했습니다. LLM이 trailing comma가 포함된 JSON을 반환하여 파싱이 실패했고, responseMimeType 강제와 정규식 후처리로 해결했습니다. Next.js rewrites 프록시가 AI 생성의 긴 응답 시간에 ECONNRESET을 발생시켜, 해당 엔드포인트만 백엔드 직접 호출로 변경했습니다. 16장의 base64 이미지가 sessionStorage 5MB 제한을 초과하여, window 객체에 임시 캐시하는 방식으로 페이지 간 데이터를 전달했습니다.
```

**섹션 5: 설계 의도** — Fill in the 3 subsections:

"왜 이 서비스를 선택했는지":
```
맞춤 동화책은 부모의 감성적 소비 욕구와 AI 생성의 저비용 구조가 만나는 접점입니다. Sweetbook API가 인쇄-배송을 처리하므로 재고 없이 주문형 생산이 가능합니다.
```

"비즈니스 가능성":
```
AI 생성 비용은 권당 수십 원이지만 실물 동화책의 감성 가치는 수만 원입니다. 형제, 생일, 기념일마다 반복 주문이 발생하는 구조로 LTV가 높습니다.
```

"시간이 더 있었다면 추가했을 기능":
```
Gemini 레퍼런스 이미지로 캐릭터 일관성 강화, 부모가 스토리를 직접 편집하는 co-creation 기능, Sweetbook Webhook으로 주문 상태 실시간 추적.
```
  </action>
  <verify>
    <automated>grep -c "문항 1:" docs/submission-log.md && grep -c "문항 2:" docs/submission-log.md && grep -c "문항 3:" docs/submission-log.md && grep -c "문항 4:" docs/submission-log.md && grep -c "왜 이 서비스를 선택했는지" docs/submission-log.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/submission-log.md contains completed 문항 1 with Phase 1~5 summary
    - docs/submission-log.md contains completed 문항 2 with SDK DX, 5-step complexity, sandbox experience, File vs Blob issue
    - docs/submission-log.md contains completed 문항 3 with JSON parsing strategy and proxy timeout decisions
    - docs/submission-log.md contains completed 문항 4 with 4 specific failure cases from troubleshooting records
    - 섹션 5 설계 의도: all 3 subsections filled ("왜 이 서비스를 선택했는지", "비즈니스 가능성", "시간이 더 있었다면")
    - Each answer is 3~5 lines per D-07
    - Tone is technical + practical per D-06
  </acceptance_criteria>
  <done>All 4 Google Form answers completed, design intent section filled, ready for form submission</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual test — dummy data order flow (D-10~D-12)</name>
  <files>docs/submission-log.md</files>
  <action>
Run the dummy data order flow manually per D-10 (dummy path only, skip AI generation) and D-11 (sandbox order with creationType: TEST). Record results in docs/submission-log.md per D-12.

Steps to execute before checkpoint:
1. Start dev server: `npm run dev`
2. Open http://localhost:3000 and verify home page loads with 5 dummy books
3. Click a dummy book and verify 16-page preview
4. Fill order form and submit
5. Check orderUid appears on completion screen
6. Add test checklist to docs/submission-log.md recording pass/fail for each step
  </action>
  <what-built>
    Full project with .env.example, README.md, and completed submission-log.md.
    Per D-10: only dummy data path tested (AI generation skipped due to rate limits).
    Per D-11: sandbox order with creationType: TEST to confirm orderUid.
  </what-built>
  <how-to-verify>
    1. `npm run dev` from project root — both frontend (3000) and backend (3001) start
    2. Open http://localhost:3000 — home page loads with 5 dummy books in gallery
    3. Click any dummy book — book preview page shows 16 pages with slider
    4. Fill order form (name, address, phone) and submit
    5. Confirm orderUid appears on completion screen
    6. Verify backend console shows Sweetbook API calls (creationType: TEST)
    7. Record pass/fail for each step in docs/submission-log.md test checklist (D-12)
  </how-to-verify>
  <verify>
    <automated>grep -c "테스트 체크리스트" docs/submission-log.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/submission-log.md contains a test checklist section with pass/fail for each step
    - Dummy book gallery loads on home page
    - Order flow completes with orderUid returned
  </acceptance_criteria>
  <done>Dummy data order flow tested end-to-end, results recorded in submission-log.md</done>
  <resume-signal>Type "approved" with test results, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `grep "문항" docs/submission-log.md | wc -l` returns 4+
- Each 문항 section has 3~5 lines of content (not placeholder text)
- No `(작성 필요)` or `*(개발 진행에 따라 자동 업데이트)*` placeholders remain
</verification>

<success_criteria>
1. All 4 Google Form answers completed with technical+practical tone (D-06), 3~5 lines each (D-07)
2. 문항 2 covers SDK DX, 5-step flow, sandbox, improvement suggestions (D-09)
3. 설계 의도 3 subsections filled
4. Dummy data flow manually tested (D-10, D-11)
5. Test results recorded in submission-log.md (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-submission-prep/05-02-SUMMARY.md`
</output>
