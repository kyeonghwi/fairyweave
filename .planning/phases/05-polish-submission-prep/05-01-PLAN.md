---
phase: 05-polish-submission-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - backend/.env.example
  - README.md
autonomous: true
requirements:
  - DEV-01
  - DEV-02
  - DEV-03
  - DEV-04
  - DEV-05

must_haves:
  truths:
    - "루트 .env.example에 5개 환경변수 키가 모두 나열되어 있다"
    - "backend/.env.example에 5개 환경변수 키가 모두 나열되어 있다"
    - "README.md의 실행 방법 섹션을 그대로 따라하면 서버가 뜬다"
    - "README.md에 Sweetbook API 엔드포인트 표가 있다"
    - "README.md에 AI 도구 활용 내역 표가 있다"
    - "README.md에 LTV > CAC 설계 의도가 기술되어 있다"
  artifacts:
    - path: ".env.example"
      provides: "Root env var template"
      contains: "SWEETBOOK_API_KEY="
    - path: "backend/.env.example"
      provides: "Backend env var template"
      contains: "SWEETBOOK_CONTENT_TEMPLATE_UID="
    - path: "README.md"
      provides: "Project documentation"
      contains: "npm run dev"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "실행 방법 섹션에서 .env.example 복사 안내"
      pattern: "\\.env\\.example"
---

<objective>
Create .env.example files and README.md covering all DEV-01~05 requirements.

Purpose: 심사자가 README만 보고 실행할 수 있는 상태로 마무리
Output: .env.example (root + backend), README.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-submission-prep/05-CONTEXT.md
@docs/submission-log.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example files (DEV-01)</name>
  <files>.env.example, backend/.env.example</files>
  <read_first>
    - backend/.env.example (current state: only GEMINI_API_KEY=)
    - .planning/phases/05-polish-submission-prep/05-CONTEXT.md (D-05 decision)
  </read_first>
  <action>
Create root `.env.example` with these exact lines (per D-05):

```
# FairyWeave 환경변수
# 이 파일을 .env로 복사한 뒤 값을 채워주세요: cp .env.example .env

GEMINI_API_KEY=
SWEETBOOK_API_KEY=
SWEETBOOK_ENV=sandbox
SWEETBOOK_COVER_TEMPLATE_UID=
SWEETBOOK_CONTENT_TEMPLATE_UID=
```

Update `backend/.env.example` to contain the same 5 keys (currently only has GEMINI_API_KEY=):

```
# Backend 환경변수
# 루트 .env에서 값을 복사하거나, 이 파일을 .env로 복사한 뒤 값을 채워주세요

GEMINI_API_KEY=
SWEETBOOK_API_KEY=
SWEETBOOK_ENV=sandbox
SWEETBOOK_COVER_TEMPLATE_UID=
SWEETBOOK_CONTENT_TEMPLATE_UID=
```

Verify `.gitignore` already has `.env` pattern (confirmed: it does, no changes needed).
  </action>
  <verify>
    <automated>grep -c "GEMINI_API_KEY=" .env.example && grep -c "SWEETBOOK_API_KEY=" .env.example && grep -c "SWEETBOOK_CONTENT_TEMPLATE_UID=" backend/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains exactly: GEMINI_API_KEY=, SWEETBOOK_API_KEY=, SWEETBOOK_ENV=sandbox, SWEETBOOK_COVER_TEMPLATE_UID=, SWEETBOOK_CONTENT_TEMPLATE_UID=
    - backend/.env.example contains the same 5 keys
    - .gitignore contains `.env` (already present, no change needed)
  </acceptance_criteria>
  <done>Both .env.example files contain all 5 environment variable keys with empty values (except SWEETBOOK_ENV=sandbox default)</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md (DEV-02~05)</name>
  <files>README.md</files>
  <read_first>
    - docs/submission-log.md (API endpoint table, AI tool table source data)
    - .planning/phases/05-polish-submission-prep/05-CONTEXT.md (D-01~D-04 decisions)
    - .planning/STATE.md (decisions log for design intent content)
    - package.json (verify dev script command)
  </read_first>
  <action>
Create `README.md` at project root. Per D-01: entire document in Korean, only code blocks in English. Per D-02: no screenshots or GIFs. Per D-03: follow this exact section structure.

**Section 1: 서비스 소개**

```markdown
# FairyWeave

부모가 아이 정보(이름, 나이, 테마, 교훈)를 입력하면 AI가 세상에 단 하나뿐인 맞춤 동화책을 생성하고, Sweetbook API로 실물 책을 주문할 수 있는 웹 서비스입니다.

**타겟:** 0~7세 자녀를 둔 부모
**주요 기능:**
- Gemini AI 기반 16페이지 스토리 + 삽화 자동 생성
- Sweetbook Print API 연동 실물 책 주문
- 더미 데이터 5권으로 즉시 체험 가능
```

**Section 2: 실행 방법 (DEV-02)**

```markdown
## 실행 방법

\```bash
# 1. 의존성 설치
npm install && npm install --prefix frontend && npm install --prefix backend

# 2. 환경변수 설정
cp .env.example .env
cp .env.example backend/.env
# .env 파일을 열어 API 키를 입력하세요

# 3. 개발 서버 실행
npm run dev
\```

프론트엔드: http://localhost:3000
백엔드: http://localhost:3001
```

**Section 3: 사용한 Sweetbook API 엔드포인트 (DEV-03)**

Table with these rows (sourced from submission-log.md):

| API 엔드포인트 | 용도 | 사용 시점 |
|----------------|------|----------|
| `POST /books` (books.create) | 빈 책 객체 생성, bookUid 획득 | Phase 3 |
| `POST /books/:bookUid/photos` (photos.upload) | 삽화 이미지 16장 업로드 | Phase 3 |
| `PUT /books/:bookUid/cover` (covers.create) | 표지 템플릿 적용 | Phase 3 |
| `PUT /books/:bookUid/contents` (contents.insert) | 16페이지 본문 배치 | Phase 3 |
| `POST /books/:bookUid/finalize` (books.finalize) | 책 확정 (편집 잠금) | Phase 3 |
| `POST /orders` | 주문 생성 (Idempotency-Key 포함) | Phase 3 |

**Section 4: AI 도구 활용 내역 (DEV-04)**

Table with these rows:

| AI 도구 | 활용 내용 |
|---------|----------|
| Claude Code (Anthropic) | 프로젝트 전체 구현: 모노레포 설정, 백엔드 API, 프론트엔드 UI, Sweetbook 연동, 테스트 |
| Gemini 2.5 Flash (Google) | 16페이지 동화 스토리 텍스트 생성 (메타-프롬프트 + JSON 출력) |
| Gemini 2.5 Flash Image (Google) | 페이지별 삽화 이미지 생성 (base64 PNG) |

**Section 5: 설계 의도 (DEV-05)**

Per D-04: 3~5줄 간결 요약.

```markdown
## 설계 의도

FairyWeave의 비즈니스 모델은 LTV > CAC 아비트리지에 기반합니다. AI 동화책 생성 비용(Gemini API 호출)은 권당 수십 원 수준이지만, 실물 책의 감성적 가치와 개인화 프리미엄이 결합되어 높은 고객 생애가치를 만듭니다. 반복 주문(형제, 생일, 기념일)이 자연스럽게 발생하는 구조입니다.

Idempotency-Key를 주문 API에 적용하여 네트워크 오류 시 중복 주문을 방지했습니다. 부모가 안심하고 주문 버튼을 누를 수 있어야 서비스 신뢰가 형성됩니다.
```

**Section 6: 기술 스택**

```markdown
## 기술 스택

| 영역 | 기술 |
|------|------|
| 프론트엔드 | Next.js 16, React, Tailwind CSS v4, TypeScript |
| 백엔드 | Express, TypeScript, tsx (dev runner) |
| AI | Google Gemini 2.5 Flash (텍스트), Gemini 2.5 Flash Image (이미지) |
| 인쇄 API | Sweetbook Book Print API (bookprintapi-nodejs-sdk) |
| 개발 도구 | Claude Code (AI 코딩 어시스턴트) |
```
  </action>
  <verify>
    <automated>grep -c "실행 방법" README.md && grep -c "npm run dev" README.md && grep -c "POST /books" README.md && grep -c "Gemini" README.md && grep -c "LTV" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `# FairyWeave` as first heading
    - README.md contains `## 실행 방법` section with `npm install`, `cp .env.example .env`, `npm run dev`
    - README.md contains `## 사용한 Sweetbook API 엔드포인트` with 6-row table including `POST /books`, `POST /orders`
    - README.md contains `## AI 도구 활용 내역` with table including `Claude Code`, `Gemini 2.5 Flash`
    - README.md contains `## 설계 의도` with `LTV` and `Idempotency-Key`
    - README.md is entirely in Korean (except code blocks) per D-01
    - README.md contains no screenshot or GIF references per D-02
  </acceptance_criteria>
  <done>README.md covers all 5 DEV requirements: env setup guide, run instructions, API table, AI tool table, design intent</done>
</task>

</tasks>

<verification>
- `cat .env.example` shows 5 env var keys
- `cat backend/.env.example` shows 5 env var keys
- `grep -c "SWEETBOOK" .env.example` returns 4
- `wc -l README.md` returns >50 lines
- `grep "##" README.md` shows all required sections
</verification>

<success_criteria>
1. .env.example (root) has GEMINI_API_KEY, SWEETBOOK_API_KEY, SWEETBOOK_ENV, SWEETBOOK_COVER_TEMPLATE_UID, SWEETBOOK_CONTENT_TEMPLATE_UID
2. backend/.env.example has the same 5 keys
3. README.md has all 5 sections matching D-03 structure
4. README.md is Korean-only prose with English code blocks (D-01)
5. No screenshots/GIFs anywhere (D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-submission-prep/05-01-SUMMARY.md`
</output>
